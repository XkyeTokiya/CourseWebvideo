# A-page v6 canonical example

这是不对应任何正式 episode 的合成输入。它故意覆盖：全局连续的 A/S/G/R/C 编号、前向 callback、`exact`/`reference`、自包含 E 引用、silent constraint、Nx 无损、短页理由和 timing。`approved-spoken-text.txt` 是与 JSON 的 Nx 拼接完全相同的公开批准稿，可直接交给现有验证入口。

测试与作者均以 `canonical-contract-example-a-page.json` 为成功样例；编译 trace 只在测试中以最小合成 B 输入验证，不把生产 episode 当 fixture。
