# Forbidden visual patterns

- 不把语义项数量直接等同于节点数量，不把受保护关系直接等同于箭头。
- “新状态成为后续数据”“结果继续反馈”等单一回返语义，默认用线性步骤与结论表达，不得据此升级为闭环图。
- 不使用复杂能力塔、贯穿脊柱、双向通道、环形轨道、齿轮闭环、断桥、长距离回流线或交叉连线。
- 不在一页同时叠加层级、分支、回流、时间轴和微型标注。
- 不用假软件界面、虚构仪表数值、企业品牌或不可验证的真实案例装饰实景图。
- 不描摹、简化重建、风格化重建或 AI 重建教材图。
- 不写图片提示词、构图细节、坐标、文件路径、裁切参数或 fallback。
