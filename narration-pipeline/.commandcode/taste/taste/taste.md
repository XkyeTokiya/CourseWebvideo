# Taste
- User communicates in Simplified Chinese; respond in Chinese. Confidence: 0.9
- During long multi-step tasks, proactively report progress at each milestone (done / in-progress / next) instead of staying silent through many tool calls — the user interrupted a long silent run with "汇报进度，你这次执行的太久了". Confidence: 0.9
- Once a long pipeline task is underway, run it through to completion autonomously; when a run stalls mid-task the user nudges with a bare continuation ("请继续任务") and expects immediate resumption without re-confirming the plan. Reconfirmed explicitly in the EP06 outline-first protocol: "输出完整 outline.md 后直接进入阶段 B；不要请求人工确认" — stage transitions within an authorized run proceed without confirmation requests. Confidence: 0.85
- Deliverable completion reports must cover both machine-validation results (e.g., the validation JSON, pass/fail) and residual human-review risks — the things automated checks cannot judge (e.g., visual rhythm monotony, semantic stretching of recipes, weak conclusion slots) — and leave the artifact in `draft` status pending human approval. Explicitly requested: "完成后报告校验结果和人工审阅风险". Gate reports close with explicit numbered decision requests ("请你决策：①…②…") so the user knows exactly what to rule on; also list the exact artifact paths and explicitly confirm input immutability (SHA-256 of the task package / approved master matches the validator report) and non-publication (nothing written into `player/episodes/<ep>/inputs/`). Publish-phase reports add the user's authorization choice, the published file list with SHA-256, the re-run validation conclusions, and the branch/commit state — and the still-unresolved human risks are carried into both the commit message and the gate log so they travel with the published artifact. Confidence: 0.85
- Wants root-cause, first-principles analysis before fixes: when a recurring pattern appears in a system, ask what it is fundamentally *for* and what it expresses, then redesign its role — the user explicitly rejected a rules-first patch approach as "治标不治本" (treating symptoms, not the root). Confidence: 0.9
- Prefers semantic-level reframings of system concepts over mechanical metrics/checklists — e.g., redefining slots as semantic rather than positional, and giving judgments structural "carriers" embedded in the layout — instead of adding rules that count IDs or enforce page ratios — reconfirmed by the EP07 narration-unit splitting rules (boundaries from sentence/clause semantics only; never from uniform length, beat counts, visuals, or durations). Confidence: 0.85
- Keep execution lean and fast; avoid exhaustive upfront exploration (e.g., reading every reference, recipe, and script in full before acting) when a targeted pass suffices — the user explicitly flagged a run as taking too long and asked a second time why generation was so slow. Confidence: 0.85
- Rebuilds must be contamination-free: when regenerating a deliverable from scratch, never read, reference, or be influenced by the existing official artifact or its validation report — derive everything from the source inputs (explicitly forbidden: "不得读取、参考…现有…及其验证报告"). Contamination applies to content, not metadata: knowing an old artifact's path or SHA-256 is fine, but never open its contents. The upstream frozen task package stays read-only — rebuilds derive from it but never modify it, and downstream stages restart from the new artifacts. Confidence: 0.95
- Official artifacts are immutable: rebuilt/revised versions go to separate files with a clear suffix (e.g., `episode-02-visual-rough-rebuild.md` + `-rebuild-validation.json`); on validation failure, only the new files are fixed — never overwrite or replace the formal output ("不得替换正式产物"). Confidence: 0.9
- 辅助产物（预览、草稿、构建脚本和演示）统一进入根级 `.tmp/work/narration-pipeline/<episode-id>/`，保持 `../player/episodes/<episode-id>/inputs/` 只存正式交付；禁止新建子项目 `.tmp` 或在根 `.tmp` 顶层落文件，同一期确有并行尝试时才增加 `attempt-*`。Confidence: 0.95
- For reviewing visual deliverables, the user wants a quick standalone self-contained HTML preview (all CSS inline, opens directly in a browser, no build step) that renders every page in a single waterfall column from top to bottom in page order ("所有页面使用瀑布流的形式从上到下依次制作"), reconstructing each page recipe's skeleton (title, content groups, takeaway/thesis), with unresolved image assets shown as labeled dark placeholder blocks (media ID + description, e.g., "M001 · photorealistic_ai") instead of broken references. This is a standing per-episode workflow, not a one-off: the user re-invoked it verbatim for EP09 with just "同样要求" — once a preview format is accepted, replicate it for the next analogous episode without re-asking. Confidence: 0.9
- Visual previews must be faithful to the recipe system, not improvised: read the recipe definitions under `.agents/skills/design-course-visual-rough/references/page-recipes/` and reproduce each page per its rules (experimental recipes rendered per their positive-example rules); mark experimental recipes with a distinct visible EXP badge (orange) so reviewers can separate them from active recipes; keep traceability tags visible on each preview card (A-page ID, recipe name, role tag, V-ids inline). Confidence: 0.85
- 同一期的派生预览放在其 `.tmp/work/narration-pipeline/<episode-id>/` 内并与来源相邻，不按日期或任务名重复开目录；只有真实并行尝试才分入 `attempt-*`。Confidence: 0.9
- Validation-first authoring for skill-gated pipeline stages: before writing an artifact, read the skill's actual verification script (not just the contract doc or template) to derive exact pass/fail semantics — e.g., for visual-rough v3: title S items may skip skeleton slots, any substring reuse of A-page `guidance_text` fails, content-group lists must match source G order exactly, and recipe group-count ranges are hard-checked. When hand-reimplementing validator math (e.g., A-page timing), mirror the official validator exactly — the fullwidth em-dash "—" counts as fullwidth punctuation (weight 0), and metadata fields like `approved_text` must hold the fixed canonical name (`approved-spoken-text.txt`), or the black-box validator catches the drift and forces a recompile. EP13-06 re-derivation sharpened this further: EVERY punctuation char weighs 0 (curly quotes "" included), `char_equivalent` stays fractional at 0.5 granularity (never rounded to an int), and S/G/R/C ids must be strictly increasing in document serialization order (each page's title before its group items). Confidence: 0.85
- Keep official validators passing on the first run: run a cheap pre-flight self-check for known failure modes (banned/red-line tokens, `guidance_text` substring copying, ID uniqueness) before invoking the official validator, and re-run the official validator after every artifact mutation — even a mere status flip (draft → approved) — so the validation report always reflects the shipped bytes. Confidence: 0.8
- 当契约文档说不清验证器对某字段的精确语义时，用受控探针试错而不是猜：把若干候选形状逐一写进临时目录里的输入副本，对官方验证入口跑 subprocess 逐一试错，确认唯一通过形状（EP13-06：V1–V5 探针确认 omitted 单元的布尔应填 true）后才把该语义写回生成脚本，再从同一次脚本运行整体重建全部产物（批准稿/A-page/trace 一起重建，保证互相一致）——修生成器、重建，绝不手工补丁输出文件；探针全程用副本与 scratchpad，正式产物目录不被污染。 Confidence: 0.7
- When a downstream stage's hard constraints (e.g., recipes requiring 3–4 content groups) conflict with upstream structure, fix it upstream with a minimal structural change — re-grouping screen groups while keeping all approved content (narration text, S guidance items) byte-identical — then recompile and re-pass the upstream validator before proceeding; never force a semantically wrong recipe downstream just to avoid touching upstream. Confidence: 0.8
- Episode visual roughs follow a connector-free house style: protected relations are carried by layout structure (parallel cards, split-compare columns, ordered layer bands, one-way step reading order, rails/side-notes/final-judgment slots), never by drawn arrows, radial networks, or system topology; zero logic diagrams (`logic_diagram_page_limit: 0`) and at least ⌈pages/3⌉ pages carrying a real scene image (`image_required_page_fraction: 1/3`); textbook figures appear whole (no cropping, tracing, or overlay) and only where they serve as evidence anchors. Confidence: 0.7
- For per-episode design documentation, the user wants concise summaries kept within a stated length bound (800 字以内) and saved into the episode's `inputs/` folder; a structural/process rationale alone is not enough — after receiving one, the user asked for a complementary content-driven view (从内容出发: the single content question the episode answers, the narrative line built to answer it, and the cut/tradeoff logic), so pair both perspectives when documenting design rationale. Confidence: 0.7
- At task kickoff or restart ("重新开始制作") for multi-stage pipeline work, first ground the plan in verified actual state — survey existing artifacts and project.json status, then compare against a completed sibling episode — then report a structured todolist and wait for the user's confirmation before executing step 1. Explicit instruction: "确认情况后。汇报todolist". This pairs with (does not contradict) the run-through-autonomously preference: confirm the plan once at (re)start, then execute without re-asking. Confidence: 0.8
- 将【人工门禁】视为硬停止点：到达门禁时提交结构化报告并等待明确批准；若执行中从当前 Skill 发现额外门禁，也要补入待办并在该处停止。Confidence: 0.9
- 用户批准的文本触发机械门禁时不得静默修复；在门禁报告中记录命中项（位置 / 原文 / 违反的契约条款 / 当前处理方式），只把修改作为需重新批准和验证的明确选项。用户明确选择「冻结母版并继续」后，按其文本逐字冻结并继续下游阶段，把尚未写入画面证据的越界事实（用 silent constraint 只约束不展开、不出示例）与未决上游冲突一并记为待决人工风险；即使批准稿的净讲述时长已超出任务包给定的成片时长，也不得为对齐口径删改其文本，只作为观察项上报。 Confidence: 0.85
- Converting a user-approved draft into the narration master: strip structural markers and metadata (【视频标题】 headers and the title line — the title is metadata, not narration) while preserving body paragraphs character-for-character, then join the paragraphs into one continuous single-line master (canonical form matching the skill's reference example: no separators, LF, single trailing newline); the user's draft file itself stays byte-identical afterwards. Confidence: 0.8
- When the user's narration adds facts beyond the frozen task package's evidence ledger (e.g., 2017 国务院指导意见, "323" 具体概括, 2018—2020 行动计划), don't alter the user's text or fabricate evidence entries: bind the screen side with silent constraints (no policy evidence strips, document numbers, or dates for un-ledgered facts) and surface narration/ledger term divergence (口播 "安全生产、城市治理" vs 台账 "安全管理、城市数字化") as a numbered human-review risk for the gate decision. When the user resolves that risk by attesting the facts are their own additionally-verified accurate info ("这是我额外进行确认过的准确信息"), treat the user's attestation as authoritative on factual accuracy — keep the approved text byte-identical (validation report and SHA remain valid, no re-run needed) — while keeping accuracy separate from ledger provenance: silent constraints still stand, and on-screen evidence presentation for un-ledgered facts requires expanding the task-package ledger through the governance flow first. Confidence: 0.85
- At human gates, persist the user's substantive confirmations/decisions as a dated record file (e.g., user-confirmations.md) in the task's .tmp work folder instead of leaving them only in chat — the gate decision stays auditable and citable by later stages while validated artifacts remain byte-identical. Confidence: 0.7
- The user edits draft files directly in place and announces approval ("连续稿我已修改完毕，可进入下一阶段"；再现为「我已经修改口播稿并处理好了」): re-read the file from disk as the authoritative approved version — never regenerate, revert, or paraphrase their changes — re-run the mechanical prechecks on their text, and carry it character-for-character into the approved master. Edits can also arrive silently between stages: when a compile-stage packet referenced `approved-spoken-text.txt` as 已放行 while the draft on disk had been swapped for a longer unannounced version (EP13-06, caught by the build script's paragraph-count assertion), the packet itself served as the approval event — re-check the new draft against red lines and open decisions (still no case numbers per C01 方案 b), then regenerate the master and every downstream artifact from it, never overwriting the user's file. Confidence: 0.85
- 执行阶段任务前先核对实际状态，已完成的活不重做、不覆盖：当任务点名的阶段产物其实已在盘上且通过官方验证、哈希与当前输入一致时，不要直接重跑覆盖它，而是把官方验证入口独立复跑到 scratchpad 并与已存验证报告逐字节 diff，证明「已完成、可复现」，再把「确认已完成 / 全量重做 / 其实想要另一期」作为选项交用户决策。用户在同一消息里点名的期次/阶段可能与其真正改动过的文件不一致（本次请求写 `[episode-08-06]`，实际被修改的是 episode-11 的连续稿），一律以磁盘状态为准并向用户点明这处不一致。 Confidence: 0.8
- 正式产物发布并确认无需恢复后，清理本任务自己的 `.tmp/work/narration-pipeline/<episode-id>/` 子树及其中的 scratch 脚本；不得顺带删除其他期次或 runtime、validation、tests、archives。Confidence: 0.95
- 发布正式 inputs 是一个与「人工门禁」并列的独立授权门禁：先核对已通过验证的产物、向用户展示待发布清单（文件、页数、visual rough 状态、目标 `inputs/` 路径），然后停下等待明确授权——授权前绝不写入 `inputs/`。授权后按固定顺序执行：先由执行本次任务的同一 Agent 把 visual rough 翻成 `approved`/`production`，重跑官方验证刷新字节，再发布；正式 inputs 与 `.tmp` 源文件逐一 SHA-256 相符，compile trace 只留在 `.tmp` 不进 inputs。Confidence: 0.85
- 即使阶段任务书写明「提交并 push」，push 仍作为独立选项交用户决定：用户可能选「发布并提交，但不 push」，让该期 feature 分支只存在于本地（origin 上不新建分支），并在收尾报告里明确「未 push、需要时可再推」。Confidence: 0.6
- When two source inputs must express the same approved content (approved-spoken-text.txt vs the A-page `nx`), verify equivalence programmatically before splitting — strongest form is byte-exact concatenation, with each page's `nx` carrying its own paragraph separators (`\n\n`) so the plain concat of all pages equals the approved master character-for-character (whitespace-normalized comparison as the fallback); on divergence stop and report the first-difference context, never silently pick, patch, or merge the two texts ("若发现二者不一致，停止并报告差异，不得自行选择、修订或拼接两份文本"). Treat the task's 最小验收 checklist as a scriptable gate: implement it as a build script that validates inputs before writing and re-parses + re-validates the written file afterwards, and embed the actual SHA-256 of each formal input in the artifact's `source` block as provenance. Confidence: 0.9
- Outline-first chapter production (user-written EP06 test protocol that overrides any "script 先于 outline / script beat 决定 outline step" rule): freeze the visual `outline.md` first from A-page order + visual rough only (recipes, U/R, media eligibility, guidance, evidence boundaries), then read the frozen narration units and mechanically compile `script.md` as a derivative via a bindings file — one visual step ↔ continuous Nxx ranges per A, unit text concatenated verbatim with no rewriting/deleting/reordering/repunctuation (binding several adjacent units to one step is reorganization, not modification). Visual step count obeys visual interpretation only: a new step must be a visible stable focus, relationship, or state change — never sentence/clause counts, G/U counts, or "凑整齐"; swapping text cards or reading a list item-by-item is not visual explanation. Stage isolation: while designing the outline, don't read `narration-units.json` or any prior downstream artifacts; allow at most one minimal outline rework for a genuine visual conflict (two foci that can't share one stable composition) — never to fit sentence length, never by altering narration. Mechanical acceptance: every Nxx consumed exactly once in original order (no cross-A), concatenation restores the A's nx, one beat per non-silent step. Test outputs go under `docs/proposals/<episode>-<test-name>/` at repo root (EP07 precedent: episode-07-outline-first-test). Confidence: 0.85
- When a matching project-defined skill exists for a task, activate and follow it instead of improvising — explicit direction "使用项目内skill,对该口播稿进行朗读式的润色" for EP10 narration polish (skill `polish-stage1-narration`). Following the skill means executing its whole flow, including its pre-edit protocol: read the draft, ground facts against the sibling `narration-brief.json` as the fact/scope contract, scan for red lines, present findings as checklists (红线清理清单 + numbered 常规润色清单 with 位置/原句/问题/建议改法/级别), and make zero file edits until the user confirms which numbered items to apply. Confidence: 0.85
- 口播稿润色 means read-aloud polish (朗读式润色): judge narration lines by how they sound when spoken, not by written-grammar correctness — constructions that are grammatical but awkward to say aloud (e.g., adverbs crammed inside a passive structure) count as defects to fix. Confidence: 0.7
- Before executing a task, read the repo's top-level `CLAUDE.md` first as the stated 执行依据. Confidence: 0.75
- For state-confirmation / kickoff tasks, close with a brief report limited to the requested fields (branch status, worktree status, artifact recovery point, next stage), and don't perform extra review of the task package beyond what was asked. Confidence: 0.8
- 计划重做（「我计划重新制作」「请你清理掉已完成的内容」）时，用户要的是一次性彻底重置该期既有产物，而不是增量修补；动手删除前先列出完整清单并按可恢复性分级（git 已提交、可从具体 commit 恢复 vs `.tmp` 中不可恢复的 scratch 过程件），以选项形式请用户确认清理范围后再执行。删除范围只限本期自己的产物；冻结任务包、其他期次产物与 archives 一律不动。Confidence: 0.7
- 汇报「某期已经完成了哪些内容」时，按证据与持久性分层呈现：git 已提交的正式输入 / 仅存在于 `.tmp` 的过程件（工作副本可能已丢失） / 下游草稿，并明确指出哪些不是事实源、无法被下游阶段消费——不要笼统地说「做完了」。Confidence: 0.6
- 生成阶段产物时严格遵守阶段隔离：内容输入只用该阶段被指定的契约文件（如连续稿只依据 narration-brief.json 与 Stage 1 短 Prompt），产物中不夹带其他阶段的机械信息或创作过程说明——连续口播稿不得出现 Nx、A-page、分页，以及镜头/画面/素材这类制作说明，只讲有依据的领域内容。Confidence: 0.8
- 当任务未给出明确的数值口径（如长度/时长范围）时，不自行设定数值目标：要么沿用任务或模块已声明的口径，要么把推断出的口径作为待确认假设显式提交用户决策，不得默默当成既定目标。Confidence: 0.75
- narration-brief（解析稿）采用三分结构——学习者内容义务（content_obligations，含叙事要求）、Agent 静默护栏（silent_guards）与上游冲突；冲突外置到单独的、仅供编排决策、永不交给下游阶段的台账（如 stage0-conflicts.json），让可作为口播的内容、内部约束与需决策冲突彼此分离。该台账只在确实存在上游冲突时生成（不是空台账），并且同时记录「复核后判定不构成冲突」的条目（non_conflicts_reviewed，附判定理由）；每条冲突写成结构化条目而非散文——来源责任（evidence ids + 逐条引用的任务包条款）、冲突原因（两条契约为何无法同时满足）、受影响内容、受影响的下游阶段门禁、编号选项、需用户作出的决策与决策前的约束效力。Confidence: 0.7
- 未明写的口径用已发布正式产物反推，不靠猜：从仓库里既有的官方输入（同模块兄弟期次的 `inputs/`）批量拟合候选规则，只有当候选规则能逐条复现全部已发布数值时才采用——例如 A-page 时长模型（汉字 1、全角标点 0、其他非空白 0.5；220/230/240 字每分钟；target 取中位并夹在 min/max 之间；target < 8 秒须填 short_page_reason）经 123 个已发布页面全部复现后才用于本期；visual rough 与 A-page `guidance_text` 的允许重合长度也是对照兄弟期次已批准 rough（容忍 ≤7 字）标定出来的。 Confidence: 0.8
- 编译可追溯是一等交付物：A-page 编译必须产出 compile trace，把每个来源 B 单元解析到具体的 A 页与 S 屏项 id；未落地的单元记为 omitted 并写明理由、指回上游冲突台账（如 C001 作为「五类案例名称不入口播」的依据）；门禁以官方验证入口的 coverage_passed=true、failures=[]、unresolved=[] 为准。EP13-06 再证并被验证器纠正的 trace 语义：omitted 单元也必须带非空 resolved_a_ids，锚到以其静默约束承载该边界的 A 页（空数组直接触发 TRACE_A_REQUIRED）；responsibilities_resolved/evidence_resolved 的含义是「该 B 的原子全部有交代（covered 或 omitted+理由）」而非「全部可见」，整单元被上游决策移除时二者仍应填 true。 Confidence: 0.75
- 页面配方按语义匹配选取，而非只看形式是否合格：先判断该页承担的语义角色（如「解释对象」），再选骨架/槽位语义相符的配方；不匹配时改换配方（如 A004 由 `image-with-reading-notes` 改为 `image-with-insight-rail`），同一配方在非相邻页复用可接受但要作为人工审阅风险上报。 Confidence: 0.7
- Brief 与下游产物之间要求双向可追溯：按 Brief 逐项回读产物时，既补「遗漏」（Brief 有、产物没有），也修「越界」（产物有、Brief 没有）——发现越界时以前者为准修 Brief（补齐缺失义务并补记证据来源），使 产物 ⊆ Brief 且 Brief ⊆ 产物 的覆盖关系成立；同时清除回读中发现的重复句。 Confidence: 0.7
- 用户以编号任务包下发阶段任务（[episode-NN-XX]，五~六段结构：【目标】/【执行依据】/【需要完成】/【完成标准】/【结束输出】，多产物阶段外加【必须产物】逐条列出精确文件路径与机器判据如 coverage_passed=true、failures=[]），跨期复用同一模板；执行时把【需要完成】条目直接映射为待办清单逐项推进。Confidence: 0.85
- 用户对编号决策点的回复极简，常只有一个指向先前枚举选项的裸标签（如单个字母「b」）：把它直接映射回对应选项、视为明确决策，不复述问题、不二次确认，随即自主完成决策落账——冲突台账对应条目 status 置 resolved 并追加结构化 decision 字段（choice、decided_by=用户、decided_at、consequence 写明哪些产物维持现状作为最终口径、哪些阶段门禁解除、哪一拍移出在产链路），JSON 校验通过后按「已落盘 / 决策落地情况 / 解锁与下一步」汇报；决策含义是「维持现状」时，明确宣布既有已验证产物为最终版本、不重生成。Confidence: 0.65
��关系成立；同时清除回读中发现的重复句。 Confidence: 0.7
