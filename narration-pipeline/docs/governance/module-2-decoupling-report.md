# 模块二“标识编码”模块间解耦改造报告

- 改造范围：第 14–20 集，共 7 个单集任务包。
- 改造日期：2026-07-28。
- 备份目录：`backups/module-2-3-decoupling-prechange-20260728/`。
- 备份内容：模块二、三共 19 个任务包、19 个口播派生物及 `course-video-progress.md`，共 39 个修改前文件；复制后 SHA-256 校验 39/39 一致。
- 基本原则：只切断模块间依赖；第 14–20 集的模块内递进全部保留；外部背景以不超过 3 句的最小必要内容内联。
- 证据边界：第 14–20 集所有 E00X 行均与备份逐行一致，未修改来源、状态、事实主张、用途或来源定位。

## 1. 逐集改动清单

以下仅列解耦与同步校验相关改动；本集身份元数据均按 A 类原样保留。

### 第 14 集

| 字段位置 | 改前 | 改后 | 类别 |
|---|---|---|---|
| 第 1 节·课程位置 | 承接第 05–06 集 | 改为模块二开篇集，并保留第 15–20 集模块内递进 | B |
| 第 1 节·前置知识 | 第 05、06 集为前置，默认已知对象身份与解析机制 | `无（本集在模块内自足）`；用 1 句交代“标识编码是物理或虚拟对象的身份符号” | C |
| 第 2 节·相邻边界 | 第 05–06 集负责系统层解释 | 改为“体系认知模块负责系统层机制” | D |
| 第 3/11 节·跨模块格式样例 | 指向第 12 集任务包及其集号 | 降级为“模块一既有已验证任务包（跨模块结构样例）” | B |
| 第 10 节·载体边界 | 载体内容属于第 33–36 集 | 改为“属标识载体模块的责任范围” | D |
| 第 11 节·去重范围 | 按第 05–06、15–20 集复核 | 改为模块级复核，并登记 1 句受控背景例外；N001–N002 已在原篇幅内自然承载背景 | C/D |

### 第 15 集

| 字段位置 | 改前 | 改后 | 类别 |
|---|---|---|---|
| 第 3/11 节·格式参考 | 指向第 12 集任务包 | 降级为跨模块既有任务包结构样例 | B |
| 第 3 节·可压缩内容 | 不展开第 21–24 集数据管理内容 | 改为“不展开标识数据管理与注册解析模块内容” | D |
| 第 11 节·去重范围 | 混入第 12 集格式复核 | 改为模块二第 14–20 集责任复核；无内联例外 | D |

### 第 16 集

| 字段位置 | 改前 | 改后 | 类别 |
|---|---|---|---|
| 第 3/11 节·格式参考 | 指向第 12 集任务包 | 降级为跨模块既有任务包结构样例 | B |
| 第 11 节·去重范围 | 只列第 14–20 集内容责任 | 显式改为模块二内部复核并登记“无受控例外” | D |

### 第 17 集

| 字段位置 | 改前 | 改后 | 类别 |
|---|---|---|---|
| 第 3/11 节·格式参考 | 指向第 12 集任务包 | 降级为跨模块既有任务包结构样例 | B |
| 第 11 节·去重范围 | 第 14–19 集责任 | 改为模块二内部责任复核并登记“无受控例外” | D |

### 第 18 集

| 字段位置 | 改前 | 改后 | 类别 |
|---|---|---|---|
| 第 3/11 节·格式参考 | 指向第 12 集任务包 | 降级为跨模块既有任务包结构样例 | B |
| 第 11 节·去重范围 | 第 14–20 集与相邻包复核 | 改为模块二内部责任复核并登记“无受控例外” | D |

### 第 19 集

| 字段位置 | 改前 | 改后 | 类别 |
|---|---|---|---|
| 第 3/11 节·格式参考 | 指向第 12 集任务包 | 降级为跨模块既有任务包结构样例；第 15–18 集模块内路径保留 | B |
| 第 11 节·去重范围 | 混入第 12 集格式复核 | 改为模块二第 14–20 集责任复核并登记“无受控例外” | D |

### 第 20 集

| 字段位置 | 改前 | 改后 | 类别 |
|---|---|---|---|
| 第 1 节·课程位置 | 不替代“后续节点注册与企业前缀申请集” | 改为模块二收束集，在本模块内闭合规则设计 | E |
| 第 2 节·相邻边界 | 第 30、32、51 集分别承担注册、闭环和前缀申请 | 改为标识数据管理与注册解析模块、节点建设与运营模块的责任范围 | D |
| 第 3/11 节·格式参考 | 指向第 12 集任务包 | 降级为跨模块既有任务包结构样例 | B |
| 第 11 节·去重范围 | 按第 30、32、51 集划界 | 改为模块级责任边界；无内联例外 | D |

## 2. 讲稿计数、时长与映射同步

计数使用仓库现有可运行脚本 `work/count_narration_equivalent.py`；任务包历史声明的三个同名验证器在仓库中不存在，因此未沿用或伪造 PASS。

| 集号 | N 段 | 字符当量 | 纯讲述 | 预计成片 | B 映射 |
|---:|---:|---:|---:|---:|---|
| 14 | 14 | 1279.0 | 5.33–5.81 分钟 | 6.03–6.81 分钟 | 精确一次 |
| 15 | 14 | 1394.5 | 5.81–6.34 分钟 | 6.31–7.14 分钟 | 精确一次 |
| 16 | 14 | 1468.5 | 6.12–6.67 分钟 | 6.52–7.37 分钟 | 精确一次 |
| 17 | 14 | 1256.5 | 5.24–5.71 分钟 | 6.04–6.81 分钟 | 精确一次 |
| 18 | 14 | 1383.0 | 5.76–6.29 分钟 | 6.16–6.99 分钟 | 精确一次 |
| 19 | 14 | 1446.5 | 6.03–6.58 分钟 | 6.43–7.28 分钟 | 精确一次 |
| 20 | 14 | 1386.0 | 5.78–6.30 分钟 | 6.43–7.30 分钟 | 精确一次 |

- 合计：98 个 N 段；第 7 节 B 表内全部精确一次映射。
- 口播文字：本模块未新增或删除 N 段；第 14 集既有 N001–N002 已满足模块首集冷启动。
- 派生物：7 份口播稿均由任务包第 6 节重新导出，未直接编辑派生文件。

## 3. 自足性验收

| 集号 | 可改字段跨模块集号 | 冷启动 | 首/末集 | 模块内递进 | 内联合规 | 计数/映射/时长 | 结论 |
|---:|---:|---|---|---|---|---|---|
| 14 | 0 | 可听懂 | 首集自足 | 保留 14→15–20 | 1 句，E002–E003 支撑，已登记 | 14 段一致 | 通过 |
| 15 | 0 | 可听懂 | 不适用 | 保留 14→15→16/19/20 | 无内联 | 14 段一致 | 通过 |
| 16 | 0 | 可听懂 | 不适用 | 保留 14–15→16→17/19/20 | 无内联 | 14 段一致 | 通过 |
| 17 | 0 | 可听懂 | 不适用 | 保留 14–16→17→18/19 | 无内联 | 14 段一致 | 通过 |
| 18 | 0 | 可听懂 | 不适用 | 保留 14–17→18→19/20 | 无内联 | 14 段一致 | 通过 |
| 19 | 0 | 可听懂 | 不适用 | 保留 14–18→19→20 | 无内联 | 14 段一致 | 通过 |
| 20 | 0 | 可听懂 | 末集在模块内闭合 | 保留 14–19→20 | 无内联 | 14 段一致 | 通过 |

## 4. 不可解耦依赖上报

无。所有外部知识缺口均可在不超过 3 句、且由既有 E 记录支撑的范围内自足交代。

## 5. 模块级独立性结论

模块二现在可以单独制作、排产、交付和从第 14 集开始学习。模块首集不再要求先看体系认知模块，模块末集不再按外部集号预告注册或节点建设内容；第 14–20 集内部的原则→体系→比较→规则设计主线保持完整。剩余外部关系仅以模块名划定内容责任，不构成学习前置。
