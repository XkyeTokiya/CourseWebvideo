# Courseplay
- Kickoff gate:正式输入集恰好是三个只读冻结源(a-page.json、visual-rough.md、approved-spoken-text.txt),任一缺失或 approved_text 不可解析即停并报告,绝不降级到通用 article 流程;用根级 `pnpm episode:new -- --id <ep> --title 「课程主题式标题」 --theme <theme>` 建实例,只把三个正式输入拷入 episode 目录(随包验证 JSON 留在后面),源 task-package 目录绝不修改;gate 判据还含冻结源 SHA256 与随包验证包逐项对账一致;实例目录已预置时(project.json+inputs 先行落位),`episode:new` 检测到 project.json 即拒绝退出、不覆盖任何文件——脚手架改从 `templates/episode` 手工补齐缺失文件(entry.tsx、src/data/cover.json、00-cover 章),跳过 project.json/script/outline/article 占位。Confidence: 0.9
- Phase 1 同轮双交付后硬停:script.md 从 pages[].nx 无损派生(批准文案不动,只加 --- 拍分隔符 + 节标题),outline.md 含 top scale stats + 全 episode 视觉调度表,先通用 outline 自检再 Courseplay 场景连续性协议(review_mode: self);包内无媒体文件时 outline 保留视觉粗设 M-id 并标 placeholder/待提供,可用的本地占位图素材库固定为 `player/占位图/16-9/` 与 `player/占位图/4-3/`,它只提供构图占位而不是事实证据或正式媒体;episode:new 后立即替换模板占位元数据(entry.tsx 真 episode id + 课程主题式标题;article.md 改写为画面信息源说明或按惯例删除);首轮实现时填 src/data/cover.json 课程元数据草稿并显式标注假定字段请用户确认;共享封面库 `episodes/_shared/covers/<ep>-cover.json` 有现成封面时,cover.json 从库文件原样复制、封面章走共享 CoverSlide(ep06 实证),字段口径随库并在 checkpoint 一并交用户确认;以 Checkpoint Plan 收尾(四计数:Base scenes / Accent frames / Custom scenes / Narration beats),等一次确认再进 Phase 2。Confidence: 0.85
- 隔离章契约:a_page.nx 是口播权威,narration.beats 是唯一允许的拍视图——narrations.ts 逐项原样复制(不改写、不重切、不增删);拍/步生成用语义(结构锚定)切分而非时间启发式:默认数量是单一确定值,每个偏差都是引用触发条件的文档化例外并内联标注,用汇总偏差统计审计;每章一个连续 base-scene,step 推进内容/焦点/关系而非机械换页;accent 步按 packet 规定组件切换,base-scene 不得重复 accent 判断;缺素材严格按 packet 占位资格处理,绝不搜索/生成替代证据;获准使用本地占位图时,按画幅从 `player/占位图/` 选文件并复制到 `episodes/<id>/src/chapters/<chapter>/assets/` 后由组件本地导入,禁止符号链接/目录联接、禁止从章节直接跨目录引用素材库,后续正式媒体在章节 assets 原位替换;仓库无共享 StatementFrame/accent 组件(grep 实证仅文档引用),accent 全屏强调结构由章内低成本自实现——含 accent 拍的章用显式 ACCENT_STEP 映射:base states 数组只覆盖非 accent 拍(按序压缩),step<ACCENT 取 base[step]、step>ACCENT 取 base[step-1]??末位、step===ACCENT 返回 accent 全屏组件,narrations 长度含 accent 拍(ep05 ch10 实证)。Confidence: 0.85
- Outline 元数据与工具解析契约冲突:修数据不修契约——handoff 生成/校验失败先 grep 工具源正则定位根因;映射/计数行严格满足契约(A-page 行须以 `Axxx` 结尾、step 时长注记用半角 `(~10s)`;工具自身 heading 全角/step 行半角口径不一时,以工具正则为契约基准);附加语义注记(如 callback)做信息移位挪独立行,绝不删改已批准文案、绝不反向改工具迁就数据;批量元数据规范化写 scratchpad 脚本一次替换并输出 before/after 计数对账;计数陷阱(用户纠正):outline.md 素材清单小节同样含 `### Axxx` 标题(带媒体的页),绝不把 grep 命中的标题当章节列表——progress.total 等计数一律完整 read outline.md 后按章节小节/视觉调度表推导(episode-04 曾凭 grep 误写 total=3,用户纠正为 A001–A008 共 8)。Confidence: 0.8
- 滚动审查簿记(chapter-review/rolling-v1):pending 登记后任何源码改动都使 source hash 失效——修复/采纳建议后统一重算 hash 再登记;attempt 文件不可变追加:must-fix 原报告保留,修复后补 recordedBy:self 验证 attempt,runtime.json 把旧 attempt 记 superseded(resultStatus: reported-late);契约审查 subagent 后台并行派发,本地同时跑构建/簿记不空等;self 验证 ≠ 终验,交付报告明示 self 验证状态、以用户视觉验收为最终关口;超协议状态(如多章 pending 突破模式 A 单 pending 上限)主动披露并附消除路径;簿记文件缺失(如 P1 漏建 review-summary.json)先读契约源(tools/*.mjs)确认期望 schema,补最小合法初始状态使验证链绿并在接续确认中披露,不改工具绕过验证。Confidence: 0.8
- 实例状态簿记同步:开工即把 project.json `status` 切到实际阶段(planned→in-progress),不留"待制作"旧态——用户会抓簿记滞后;合法取值先从 validator 源(validate-episodes.mjs)+ sibling 实例现状推导再写,不凭猜测;契约门控字段(rolling-v1 `current` 须对应 settled/已登记 pending,而 pending 登记要求正式章目录+entry.tsx 注册)在隔离期保持最小合法态(`current: null`),完整值留到门控事件(验收迁移+pending 登记)同轮落,并在汇报中说明该口径;每次状态改动后重跑 episode:check 确认绿;`progress.total` 口径=正文 A-page 章数+1 封面章(ep35:11 正文+封面=12;ep05:10+1=11),不等于 outline 顶部"章节"数(只数正文)。Confidence: 0.8
- 标题省略授权(实验性、仅删除性):语义冗余的论点标题可在 outline 层省略——可见标题: `none` + 必填省略理由,核心判断保留为 outline 元数据;章代理严格执行 outline 声明,无新增判断权;"省略后观众仍能从当前既有画面完整得到同一判断"必须为明确 yes,有疑即保留;在 1-2 个 episode 试点记录哪些页通过/不可省略(标题是唯一判断载体的页)后再谈扩展。Confidence: 0.85
- 风格参考 episode 取用序与参考边界:新实例章节开发的风格参考默认用 episode-35(用户指定为本期基准),用户显式指定的实例永远优先,绝不按"最近完成/最新"等启发式自行挑选;参考边界是只深读单一完整 exemplar 章(tsx/css/narrations 全量)+ 契约文件(entry.tsx 注册接法、outline 字段口径),刻意不广读其余 sibling 章——视觉多样性来自让 agent 只对齐机制(states 数组+`?? last` 兜底映射、`--x-i` stagger 变量、data-tone/data-on 状态属性、card-glass 判断 plate、CSS keyframes+animation-delay calc)而非对齐样子。Confidence: 0.85
- 上屏 guidance 用法(用户定规,已明文重申):tag 式上屏的根因就是把 `episode-04-a-page.json` 的 screen 字段(S/G 上屏内容组条目,handoff 投影为 packet 的 screen_guidance)机械映射为屏上组块——用户明文定规:a-page 的 screen 只是内容方向参考项,不是逐条落屏清单,下游必须综合三源(screen_guidance + narration.beats + presentation)重新设计完整上屏内容;禁止按 G/S 分组机械映射成屏上组块、tag 或一 S 一块,且不保留 guidance 的清单结构(把 chips 换成箱体/列表行的"换皮"重做因信息组织仍按 S/G 分块,被判"治标不治本");skill 标注为"参考项/可选"的字段从设计阶段起就按参考对待,绝不把参考当逐条清单。Confidence: 0.95
- 用户附带辅助文档的取用边界(episode-06 开工指令):随输入一并提供的解说性文档(如 `episode-06-a-page-content-notes.md`)只可作理解上下文——不是强制参考、不是制作目标;制作产物的结构/数量/口径约束只来自冻结正式输入与用户显式指令。用户未明示地位的附带说明文档默认按此处理,并在 checkpoint 报告披露其实际用法(仅作理解参考、未作为参考目标)。Confidence: 0.7
- Outline 章设计块权威(R3/R4 验收裁定):outline.md 每页的设计要求是章实现的硬约束——可见标题声明(如 S010/S014"可见标题: 保留")与结构指纹(如 A003 `steps ×2 | terminal-result`、A004 `parallel-cards ×3` 等权三卡 `equal-weight-accumulation`)必须原样落成屏上结构;screen_guidance 三源重组只发生在 outline 声明的槽位内部,不得以"脱离 S/G 结构"为由丢掉标题区或把等权三卡改成横向流水线——用户裁定此偏离为"本质上没有重做,完全偏离 outline 设计要求,验收不通过";弃 guidance 结构 ≠ 弃 outline 结构,handoff packet 对 outline 只带哈希不带章设计块内容,章制作前必须直接读 outline.md 本章设计块。Confidence: 0.9
- 工具投影防诱导(设计缺陷倾向,修复方向待用户裁决):agent 层错误要继续追因到工具对上游参考数据的投影方式——用户会追问"这是否是设计缺陷";须分层如实诊断:"规范缺口"(限制条款是否覆盖实际在用的字段,本例 screen_guidance 已被禁止机械对应条款 + usage_policy:reference 覆盖)与"工具设计陷阱"(a-page screen 的 G/S 清单结构逐条原样搬运进 packet,天然诱导按组落块/按条落 tag)可同时成立,契约文本已写 ≠ 无设计缺陷;参考数据的更优投影是信息移位而非结构搬运(压缩为整页内容方向摘要 + 事实边界,或字段级显式反逐条警示);修复按"只修实现 / 改工具投影 / 两者都做"给选项等用户裁决,契约层改动不自行动手。Confidence: 0.7
- 验收连续否决→回退里程碑(用户裁定):同一实例多轮重做(如 R2→R4)仍被判"依旧无法认同"时,用户的处置是全量回退到最近已接受里程碑状态(如 P1)而非继续修补——清理该实例本会话的全部制作产物与中间产物:章目录与 entry.tsx 注册回退(仅封面)、.handoffs 包、tmp 隔离目录、audio 段重提取;里程碑自身的产物(outline/script/冻结输入/封面)原样保留,progress.completed 归 0、其余簿记口径不动;仍按 scoped commit 落盘并复跑验证链。Confidence: 0.75
- 章节动画时序绑定:状态相关动画(step 切换才出现的元素)必须把 animation/transition-delay 挂在状态 class/属性选择器下(如 `.ps-q.is-on .ps-q-plate`、`[data-on="on"]`),绝不能挂在元素本身——常驻 DOM 元素的动画在 mount 时即计时并跑完一次,state 切换不重跑,delay 形同虚设(ep04 A001 浮层两次返工案例);进章即播的元素(照片入位、badge)挂元素本身是正确形态。Confidence: 0.9
- 弱化/聚焦过渡的方向化延迟:`is-weak` 类弱化态的 transition-delay 只写在弱化态规则里(如 `.ps-main.is-weak { transition-delay: 2600ms }`),基础规则保持 0——正向进入按延迟编排,反向导航/回退即时恢复,不污染离开路径;ep35 第1章复发该反模式(`.pq-question` 700ms delay 写在基础 transition 上,回退也被延迟 700ms),经用户委托的过拟合裁决判机制规则胜过点实现后当场修复(移入 `.is-on`),跨 episode 二次实证;subagent 所写其余章的同类模式列为批量自查项。Confidence: 0.9
- 落位态可见性显式兜底(ep06 ch8 实证机制反模式):元素基态 `opacity: 0`("待落位"骨架槽惯用 blanket `.x-core > * { opacity: 0 }`)配 `backwards` 填充入场动画时,keyframes 必须有显式 `to` 帧(或落位态规则显式补 `opacity: 1`),否则动画结束后计算样式回落基态 opacity:0——内容入场一次即消失,视觉症状是"整章只剩标题与标注,框架还停在骨架透明度";修复只补落位态可见性,骨架态与状态门控结构不动;配套两点:blanket `> *` 基态隐藏会无差别命中已落位内容,骨架意图不得由它承载;状态级联选择器收窄到目标元素本体(如 `.is-national .gn-band-national .gn-band-core > *`),宽后代选择器会在换步时误重触发早拍内容的入场动画。Confidence: 0.8
- 拍内入场链不越拍:逐条级联入场的清单里,给末项设的专属延迟不能远离级联尾巴——前三问 ~0.7–2.4s 落完、末项再孤立等 1s(3.4s)才进,观感上被用户误判为下一 step 的内容(用户报 bug 后自行撤回“可能是出现太慢了”);“口播末句最后入场”的顺序保留,但末项应紧随级联(+500ms 量级)在拍口播窗口内收完(全链 ~3s),延迟把条目推离级联一个空拍即为越拍观感风险(ep05 ch3 实证)。Confidence: 0.75
- 进场时刻锚定口播报到点:拍内元素的落位/实体化应安排在口播报到它的时刻,而非紧跟兄弟元素级联+小偏移——ep05 ch4 occ 带三与带二同拍触发仅晚 640ms、~2.3s 内全链放完,口播 ~3s 后才讲到载体,拍内剩余 7s+ 无事发生,用户感知“出场很快但延时进下一步”并猜测动画写错了地方;症状“秒进+迟迟不进下一步”先对 narrations 时间轴与 CSS delay 排程对账定性,再考虑已知反模式(动画挂元素本体/transition-delay 写基础规则);修复=方向化延迟(同 pcs/bcg 既定模式):虚线槽位保持到口播报到点(实例 ~3.2s)再成形,transition-delay 只写落位态保证回退导航即时还原槽位,内容级联(正文→载体块→码格/条码→吊牌)整体重锚随后正常走完、收在本拍内;秒级长延迟落位不用位移式入场——延迟期内 translateY 起始帧悬空可见(实例 26px),改槽位原位成形(虚线→实线+底座投影,序号变色等配套态同步延迟)。Confidence: 0.8
- 场景语义状态与拍时长实践(episode-35 实证,用户评定成品“远超预期”):语义状态按“结束时的稳定画面”命名(如 question-dominant、combo-concluded),step 映射到 state 数组而非把 step 当 active 索引,天然避免 PPT 化实现;beat 切分偏离(合并/扩张/并入)用 230字/分 估算拍时长前置拦截 <4s 短拍陷阱,偏离依据为“独立口播焦点”或“<4s 短拍”并内联标注;整集视觉调度表保证相邻页构图互不雷同,是“像视频不像 PPT”观感的关键。G 组数只是结构参照,绝非拍数、更非“1 组留 1 拍”的豁免——拍切分权威是独立口播焦点+稳定画面,把 1 组页收成单个 30s+ 长拍被用户判“当前 step 切分无法验收”并整集重切(ep05);拍密度以脚本实证基准实例为准(ep35 拍数/G 组双向偏离 2/1、3/4、4/1,拍均 ~10s、区间 4~15s、<4s 记 FAIL——数字为基准参照非硬数),口径出处悬空(如 BOUND-MODE 引用的 §1.4 全仓无出处)时同样用脚本实证基准实例推导口径,不猜;该口径与 BOUND-MODE「默认=G 组数」条款文本构成已备案契约冲突(两侧皆合法:条款本意或是防机械压缩、实证口径是防粒度不足),调和方向=上游修订条款措辞——属冻结 skill,按上下游边界备好修订文本交用户裁决授权,不单方面改;episode-06 开工时用户以指令对该冲突作出行为裁决:生成 outline.md 时**不允许将【G组数量】作为【生成对应数量 step】的豁免**——即便数量恰巧吻合,步数仍须从独立口播焦点切分推导,G 组只作 beat 锚定参照,偏离内联标注(skill 冻结文本本身仍不动)。Confidence: 0.9
- Handoff 工具取舍(episode-35 实证):主线程已持有全局上下文(读完 A-page JSON + visual rough 写出 outline)并统一派发并行 subagent 时,不逐章跑 `pnpm courseplay:handoff`——内联 prompt 等价于一次更精准的手工 handoff(“裁剪+翻译”:guidance→构图指令、silent_constraints→护栏清单,agent 拿到的是消化后的规格而非原始素材),还省掉逐页生成/校验往返;何时该改用 handoff 属未经实证的构想(用户明示“这是你的构想”),不预写适用场景,出现真实交接需求时再评估;handoff 的上下文隔离思想以轻量形式落地(episode-35 实证):背景与规范不作整份复制,按“文件路径+节标题”指针分发让 agent 自读所需节(outline.md 指定「## 4.」「## 5.」两节、第 1 章 tsx/css、CHAPTER-CRAFT.md、A-page JSON 只读自己页),prompt 内联的只是消化后规格+逐字 beat——内联管“少而精、不容歧义”,指针管“多而稳、按需读”,打包动作由读者执行而非打包者;推论:该流程的质量闸门是 outline 视觉层——内联会把 outline 的糙原样放大;ep05 六 agent 复制实证:零结构偏离、零结构提问(states 映射与 outline 全一致、narrations 逐字==nx),残余摩擦几乎全为设计边缘判断并按只报告契约上交——内联消化确证把 agent 摩擦从结构歧义移向设计判断。成本模型(ep05 实测,数值属实例参考):总成本=固定必读采集(指针文档集,与任务难度无关;被中断 agent 15 次调用 0.81M 全花在读上是干净数据点,大头是 craft 全文必读)+ 可变成本∝拍数×自绘演示密度×CSS 体量+规定自检+报告;时长与 token 消耗不同源(少回合长思考/长文件生成 vs 多次自检调用),评估 agent 效率两者分开看;成本归因观测边界:读不到 agent 内部逐条工具日志,以 usage 块+交付报告自述+落盘产物三方交叉推断,数据点直接支撑与合理推断分开标注;降本候选(未实证构想,仅供裁决):craft 必读改节选指针(仅原则/红线/自检三节)约省 0.3-0.5M/agent,质量风险未验证——建议下集试点一次再议固化。Confidence: 0.75
- 设计资料取用层级(episode-35 实证):视觉制作的设计权威只用三层——域 skill(web-video-presentation)的 references 子文档(CHAPTER-CRAFT / BOUND-MODE / STATE-MECHANISMS 等)、仓库主题系统(theme.json+tokens.css 的 primitive 是视觉统一感的真正来源)、已验收风格锚章代码;不引入通用设计 skill(design、ui-ux-pro-max 等)——仓库自身约束(16:9 舞台、token/primitive 禁令、主题性格)比通用设计指南更具体,套用反而冲突。Confidence: 0.6
- Build 媒体落位核验(shared 桶,ep05 实证):episode 媒体与他集字节相同(同源占位图素材必然如此)时会被 Vite 字节去重落入 `dist/media/shared/`,因此 `dist/media/episodes/<ep>/` 无目录 ≠ build 异常——定性前先查 `dist/manifests/assets.json` 归属登记与 `docs/media-build-layout.md` 的 shared 桶契约语义(契约内设计,build 会拒绝违规落位),归属登记正确即不修,不为非缺陷做修复。Confidence: 0.7
- 章尾形态与收尾 accent 映射(ep05 ch10 用户裁定):用户明示删除"下一集入口"门控块(los-gate 整块:TSX 元素+全部样式+keyframes+gateway-open/is-gate/is-return 死状态,残留引用 grep 自查后跑链)并要求"最后一个step保持和上一step一致,也就是全屏总结页"——章尾不引入新视觉元素(下集预告/入口类),accent 拍后的剩余拍一律持续全屏 accent 总结页;实现口径:ACCENT_STEP 映射取 `step >= ACCENT_STEP` 直接返回 accent 组件(不再回 base——区别于 accent 居中段章的 `step===ACCENT` + `step>ACCENT 取 base[step-1]` 口径),机制依据是同章跨步组件实例持续(`<Component step={...}/>` 无 key),重复渲染同一 accent 组件即画面延续、入场动画不重放,无需 is-return/animation:none 类补丁;narrations 冻结契约不动——被删视觉对应的口播仍会在全屏总结页上播放,交付报告披露一次、去留交用户裁决。Confidence: 0.75
�在全屏总结页上播放,交付报告披露一次、去留交用户裁决。Confidence: 0.75
